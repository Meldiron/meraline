## What this does

<!-- One or two sentences on the change and the problem it solves. Link the issue if there is one. -->

Fixes #

## How I tested it

- [ ] `scripts/test.sh` passes
- [ ] Tried it in the app with `scripts/dev_run.sh`
- [ ] For pipeline or packaging changes: `scripts/release.sh 0.0.1 --adhoc` passes

<!-- Screenshots or a short recording for anything visible. -->

## Checklist

- [ ] Nothing new is written to disk, and secrets stay in the Keychain
- [ ] Logging goes through `Log`, with no questions, answers, or keys in messages
- [ ] Pink appears only as an accent (see AGENTS.md)
- [ ] `project.yml` is updated if files or dependencies changed
